# Giovanni feedback implementation and validation

Local validation, 19 September 2026. Changes are not a published release.

## Requested changes

| Request | Implementation and evidence |
| --- | --- |
| Keep variable-length CND trials as separate MNE Raw objects | Existing behaviour retained; original-trial metadata does not reorder trials. |
| Clarify catalogue verification | The saved 18 reports cover 1,026 subject files / 17,774 trials. 1,025 created MNE objects and passed shape/value and in-memory round trips; 1,017 passed PSD checks. Eight files contain no samples; one failed to load. These reports do not claim catalogue-wide serialized MAT round trips or full scientific analyses. Unit `V` was an explicit testing assumption, not verified physical calibration. |
| Explicit units | Neural and EOG units are explicit. No source calibration is inferred. External MNE inputs must share the neural time origin. |
| Check coordinates visually against device specification | Labelled Lalor 128-channel CND/MNE plot compared directly to BioSemi's `Cap_coords_all.xls`, `128-chan` sheet. Maximum directional discrepancy 0.000349 degrees; median 0.000180 degrees. Geometry matches to rounding precision. Both plotted on a 95 mm sphere for comparison; this does not validate physical head size. See converter `docs/results/giovanni-review/`. |
| Preserve separate clocks | Storage supports distinct sampling rates with a shared sample-zero assumption. Feature event collisions fail explicitly. Actual experimental synchronisation remains a source/recipe responsibility. |
| Preserve trial order | Stored order is retained; `origTrialPosition` is metadata. |
| EOG as external channels | All six ERP CORE recipes retain EOG, with names/types/units. Exports use MATLAB cell groups so upstream `eeg.extChan{1}.data{1}` works. |
| Explain one-based samples | Source index 1 corresponds to Python index 0; subtract one before indexing. |
| Configurable envelopes | Explicit Hilbert and rectified choices, compression, normalisation and feature rate. Unsigned PCM is centred. Audio offset trims the audio start. The real speech-dataset recipe remains draft pending reviewed audio/trial mapping. |
| Run actual mTRF code | The final 240-recording corpus passed configured CNSP preprocessing and held-out TRF prediction in MATLAB R2026a using unchanged upstream functions. Details below. |
| Demonstrate CND→MNE | Labelled sensor plots, 1–40 Hz filtering, waveform and PSD figures generated by the converter's `examples/validate_giovanni.py`. |
| Functions or GUI? | Python functions and notebook; a GUI/MNE upstream integration is not implemented or implied. |

## Two compatibility bugs found by actually loading the MAT files

1. Scalar stimulus features had been written as `1 × time` rows. Python's reader
   squeezed these into vectors, hiding the error. They now serialize as
   `time × 1` columns, as the upstream scripts expect. Pipeline features explicitly
   use columns for compatibility with its older pinned converter dependency too.
2. Newly exported external channels were a plain MATLAB struct. Upstream CNSP
   accesses cell groups with braces. New exports now use a cell containing a group
   struct. Legacy input layouts remain supported. When editing an existing
   single-group template through MNE, pass `cnsp_external_cells=True` to
   `write_cnd` to explicitly normalize it; the default preserves its layout.

Regression tests inspect the serialized MATLAB types and dimensions directly.

## Real-data scope

All 240 checksum-pinned ERP CORE recordings were rebuilt with the final
column/cell corrections: 40 people across MMN, N170, N2pc, N400, P3 and
flankers. Every recording passed source reconciliation, strict CND checks,
MATLAB read-back, EEG/EOG numerical comparisons through MNE, coordinate round
trips and event checks. The final corpus contains 139,146,240 samples at 1024 Hz
(37.75 hours), with 30 EEG and three external EOG channels per recording. Its
canonical content hash is
`a8d604f2cc09c55fdb7afbc86973771c6142a4363930d772de015cf786814c68`.

## Upstream TRF execution

Source: https://github.com/CNSP-Workshop/CNSP-resources

Pinned revision: `74d26e5b7ded8eb80f56aa26517b7e73ba4a83a7`.
No upstream source files were edited. A separate harness calls the bundled
`mTRFpartition`, `mTRFcrossval`, `mTRFtrain`, and `mTRFpredict` functions.
The entire continuous run is divided into five analysis blocks; four are used
for cross-validation and training, the fifth is held out. Storage remains one
trial. Lags are 0–200 ms, lambda candidates are 0.1, 1, 10. Raw source sampling
rate is retained; no unfiltered decimation is performed.

| Pilot | Samples | Feature events | Channels | Held-out mean r |
| --- | ---: | ---: | ---: | ---: |
| MMN | 622,592 | 785 | 30 | -0.002737 |
| N170 | 699,392 | 80 | 30 | -0.020976 |
| N2pc | 812,032 | 160 | 30 | 0.016043 |
| N400 | 585,728 | 60 | 30 | 0.022304 |
| P3 | 478,208 | 40 | 30 | 0.016406 |
| flankers | 935,936 | 100 | 30 | 0.010092 |

These are execution/format compatibility checks, not validated scientific models.
No significance, predictive utility or optimised preprocessing is claimed.
Reports and per-task logs are in `giovanni-validation/`; `run-summary.json` records
input SHA-256 hashes, upstream revision, feature choices and exit codes. MMN uses
feature 2 (`standard_onset`): its first feature has insufficient event coverage
across all five blocks. Other pilots use feature 1.

Reproduce from the Research workspace after creating the converted fixtures:

```sh
cd open-neurodata-to-cnd
python3 scripts/validation/run_validation.py --data-root ../tmp/giovanni-validation --upstream ../tmp/CNSP-resources --output docs/reports/giovanni-validation
```

The harness also uses MATLAB-compatible syntax, but was executed in Octave only.

## MATLAB preprocessing and TRF validation

All 240 final recordings passed `scripts/validation/check_matlab_cnsp.m` using
MATLAB R2026a Update 5 with Signal Processing Toolbox. The driver calls unchanged
upstream CNSP functions for 8 Hz low-pass filtering of EEG and EOG, downsampling
to 64 Hz, 1 Hz high-pass filtering, bad-channel detection/interpolation,
average referencing, and stimulus downsampling. It then runs mTRF
cross-validation, training and held-out prediction with lags -100 to 600 ms.
All fitted weights and reported correlations were finite. The 240 held-out mean
correlations ranged from 0.0245 to 0.6922; these values are execution evidence,
not scientific results.

These are compatibility checks, not evidence of statistical significance or
validated predictive utility. Each recording contributes four training blocks
and one held-out block; these are not independent participant-level evaluations.

The original tutorial scripts have not been run unchanged. This configured
driver makes the dataset-specific choices Giovanni asked us to identify:

- Local dataset paths and platform-compatible path separators.
- Average reference instead of the tutorial's mastoid assumption: ERP CORE's
  external channels are EOG and must not be treated as mastoid references.
- All declared stimulus features as separate predictors. A single feature can
  be absent from an equal time block in some recordings, which makes that split
  unsuitable for cross-validation.
- Removal of the idle tail after the final event while retaining the complete
  600 ms positive-lag response window, followed by five analysis blocks.
- A common 64 Hz analysis rate. These pilots start at 1024 Hz, giving the integer
  ratio required by upstream `cndDownsample`. Arbitrary separate source clocks
  still require an appropriate resampling choice.

No upstream source was edited. Logs, input SHA-256 hashes, pinned upstream
revision, MATLAB version and per-recording results are in
`giovanni-validation/matlab-full-corpus/`. Reproduce from the pipeline repo:

```sh
PATH=/Applications/MATLAB_R2026a.app/bin:$PATH python3 scripts/validation/run_validation.py --data-root outputs/nm000132/0.4.0/recordings --all-recordings --upstream ../tmp/CNSP-resources --output docs/reports/giovanni-validation/matlab-full-corpus --engine matlab --preprocess
```

The original tutorial scripts themselves are interactive and path-specific; the
driver preserves the upstream processing/model functions and records the
dataset-specific choices required to run them on ERP CORE.

## Coordinate export and final checks

New exports from the local converter include EEGLAB `sph_theta`, `sph_phi`,
`sph_radius`, `theta`, and `radius` as well as Cartesian coordinates. Their
polar values were independently checked in Octave against unchanged upstream
EEGLAB `convertlocs(..., 'cart2all')` on the rebuilt MMN pilot (tolerance 1e-10).
The pipeline's older pinned dependency does not yet include this companion
improvement; use the local converter for the complete combined behaviour until
an updated converter revision is published.

Final checks: converter 131 tests passed; pipeline 50 offline tests and both
public-data integration tests passed with the local converter.
Ruff, mypy and diff whitespace checks passed. The original walkthrough notebook's
pre-existing edits were preserved; the new demonstration is a separate script.
