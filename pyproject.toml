[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "open-neurodata-to-cnd"
version = "0.1.0.dev0"
description = "Reproducible conversion of public structured neurophysiology data to CND"
readme = "README.md"
requires-python = ">=3.10"
license = "BSD-3-Clause"
authors = [{ name = "Finn Clancy" }]
dependencies = [
  "mne>=1.8",
  "numpy>=1.26",
]

[project.optional-dependencies]
conversion = [
  "cnd-mne-converter @ git+https://github.com/finnjclancy/cnd-mne-converter.git@1b48b432aa127a46d5962e7b652ab4b04deb3754",
]
dev = [
  "cnd-mne-converter @ git+https://github.com/finnjclancy/cnd-mne-converter.git@1b48b432aa127a46d5962e7b652ab4b04deb3754",
  "edfio>=0.4",
  "jsonschema>=4.23",
  "mypy>=1.18,<2",
  "pybv>=0.7",
  "pytest>=8",
  "pytest-cov>=5",
  "ruff>=0.9",
]
test = [
  "edfio>=0.4",
  "jsonschema>=4.23",
  "mypy>=1.18,<2",
  "pybv>=0.7",
  "pytest>=8",
  "ruff>=0.9",
]

[project.scripts]
neurodata-to-cnd = "neurodata_cnd.cli:main"

[project.urls]
Repository = "https://github.com/finnjclancy/open-neurodata-to-cnd"

[tool.hatch.build.targets.wheel]
packages = ["src/neurodata_cnd"]

[tool.hatch.metadata]
allow-direct-references = true

[tool.pytest.ini_options]
addopts = "-ra --strict-markers"
testpaths = ["tests"]
markers = [
  "integration: downloads and converts a pinned public source file",
]

[tool.ruff]
line-length = 88
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

[tool.mypy]
python_version = "3.12"
ignore_missing_imports = true
check_untyped_defs = true
no_implicit_optional = true
warn_redundant_casts = true
warn_unused_configs = true
warn_unused_ignores = true
